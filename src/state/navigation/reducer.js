import { ACTION_FOO } from './actions'

const initialState = {
    current: 'Login',
    history: []
}

function reducer(state = initialState, action) {
    switch (action.type) {
        default:
            break
    }

    return state
}

export default reducer
